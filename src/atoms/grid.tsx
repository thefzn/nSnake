import { styled } from 'linaria/react'

const grid = styled.div`
	border-top: 1px solid rgb(159, 154, 180);
	border-left: 1px solid rgb(159, 154, 180);
	display: flex;
	flex-wrap: wrap;
	width: fit-content;
`

export default grid
